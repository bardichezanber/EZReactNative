import React, {
  AppRegistry,
  StyleSheet,
  Text,
  View,
  Navigator,
} from 'react-native'
import MainHandler from './handler/main'

AppRegistry.registerComponent('EZReactNative', () => MainHandler);
